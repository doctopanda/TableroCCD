{
  "name": "dashbash-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "cd ../frontend && npm install && npm run build && rm -rf ../backend/frontend-dist && cp -r dist ../backend/frontend-dist",
    "kill-port": "kill-port 3000",
    "dev:clean": "npm run kill-port && npm run dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.31.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "nodemon": "^3.1.10"
  }
}
